# Dodu — Codex 작업 지침

## 적용 범위
이 문서는 Dodu v2.5 작업용 지침이다. CareBell 등 다른 프로젝트의 기술 스택이나 기능을 Dodu에 자동 적용하지 않는다. 사용자의 현재 요청 및 기존 저장소 지침을 함께 확인한다.

## 작업 시작 시 읽을 문서
0. 사용자 지정 Notion 기준 페이지: https://app.notion.com/p/3d64c293890d80cc940bdc3cef7aa32c 및 하위 페이지·데이터베이스 항목을 매 Dodu 작업 시작 시 다시 읽어 최신 변경을 확인한다. 작업 중 업데이트 통지를 받으면 관련 하위 항목까지 다시 확인한다. 접근 실패 시 읽었다고 주장하지 말고 확인하지 못한 범위를 보고한다. 상시 자동 감시를 구성한 것으로 간주하지 않는다. 최신 문서의 제안이나 OPEN 항목도 승인 근거 없이 확정 정책으로 취급하지 않는다. 기존 Baseline과 충돌하면 출처와 승인 상태를 구분해 보고한다.
1. `docs/dodu/IMPLEMENTATION_CONTEXT.md`
2. `docs/dodu/OPEN_DECISIONS.md`
3. 작업에 관련된 원문: `docs/dodu/sources/PRD_v2.5.md`, `SERVICE_POLICY_v2.5.md`, `FLOW_LOG_MAPPING_v2.5.md`
4. 검증 시 `docs/dodu/ACCEPTANCE_CHECKLIST.md`
5. 작업에 관련된 원문은 업데이트 될 수 있으므로, notion 기준 페이지를 작업을 진행하는 해당 일자의 최신 버전을 참조한다. (일 1회)

원문을 읽지 않고 요약만으로 정책을 구현하지 않는다. 문서의 ‘1차 배포’는 기준 문서 공유 단계이며 제품 배포 지시가 아니다.

## 근거와 권한
- 제품 목적·범위·가설은 PRD, 시간·횟수·권한·보관 정책은 서비스 운영 정책, 이벤트·계산식은 로그 매핑을 기준으로 한다.
- 원문은 전달받은 Baseline이며 이 패키지의 파생 문서는 구현 참고 자료다. 파생 문서가 원문을 덮어쓰지 않는다.
- OPEN_DECISIONS의 제안은 승인된 정책이 아니다. 미결정 항목과 연결된 사용자 동작을 임의 확정하지 않는다.
- 미결정 항목과 무관한 환경 조사·기반 구현·정상 흐름 작업은 계속한다. 결정이 필요한 지점과 영향 범위를 짧게 보고한다.
- 운영값을 바꾸거나 과거 v2.3.1/v2.4.1의 손실·포인트·등급 기능을 되살리지 않는다.

## 제품 불변 조건
- 인증 완료는 실제 작업 시작의 증거가 아니다. 시스템 인증과 자기보고를 별도로 저장하고 표시한다.
- 미응답은 미시작이 아니다. 기술 실패·취소·철회를 인증 미완료에 합산하지 않는다.
- 알림 발송은 수신·열람의 증거가 아니다.
- 재알림·화면 재진입은 인증 시간을 연장하거나 세션을 새로 부여하지 않는다.
- 마감 전 정상 접수된 유효 사진은 마감 후에도 판정 결과를 기다린다.
- 사진 인증 횟수 제한을 사용자 정책으로 추가하지 않는다.
- 설문 미응답이 다음 약속 생성을 차단하지 않게 한다.

## 저장소 조사와 구현
- 매 커밋 전에 루트 `Decisions.md`에 해당 결정의 배경·선택·근거·대안·영향·검증을 기록하고 같은 커밋에 포함한다. `Co-authored-by` 등 공동 작성자 표기는 추가하지 않는다.
- 코드·문서 작업에는 `docs/DEVELOPMENT_CONVENTIONS.md`의 개발·검증·Git 규칙을 함께 적용한다. 제품 정책과 OPEN 결정의 권한을 개발 컨벤션으로 덮어쓰지 않는다.
- 먼저 기존 AGENTS.md, README, package.json, lockfile, 앱 구조, API·DB·알림·배포 설정을 확인한다. 실행하지 않은 테스트나 확인하지 않은 기술 스택을 사실처럼 말하지 않는다.
- React/RN/Next.js, DB, 알림 제공자, AI 모델, 배포 환경은 이 패키지에서 확정하지 않는다. 실제 저장소를 우선한다. 플랫폼 Baseline은 웹앱/PWA다.
- 기존 주석을 보존한다. 기존 코드 변경·삭제 시 사용자 선호에 따라 이전 코드를 문법에 맞게 주석 처리하고 새 코드를 추가한다. 변경 범위는 최소화한다.
- MUI를 실제 사용하는 경우 설치 버전을 먼저 확인한다. MUI 7 프로젝트에는 v7 공식 문서를 적용하며 Grid의 breakpoint 키는 `xs`다. 이 문서만을 이유로 MUI를 도입하지 않는다.
- 서버를 시간·상태 검증의 최종 기준으로 삼는 설계를 제안한다. 이는 기술설계 제안이며 API/DB 구조는 실제 저장소에 맞춘다.
- 중복 요청, 수정 전 알림 작업, 지연된 AI 결과, 철회 후 결과가 최종 상태를 잘못 덮어쓰지 않게 한다.
- 민감한 사진·참가자 접근정보·설문 원문을 디버그 로그나 코드에 노출하지 않는다.

## 검증 및 보고
- 정책 경계·상태 충돌·데이터 연결처럼 실제 위험이 있는 동작을 검증한다. 문서 수정만으로 앱 테스트 성공을 주장하지 않는다.
- 결과에는 변경 파일, 근거 문서/절, 확인한 동작, 수행한 검증, 남은 결정 ID를 포함한다.
- 원문 파일은 변경하지 않는다. 합의된 정책 변경은 별도 결정 기록에 근거·담당·승인일을 남긴 뒤 관련 문서를 동기화한다.
